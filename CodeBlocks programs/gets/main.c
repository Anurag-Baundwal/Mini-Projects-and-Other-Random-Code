#include <stdio.h>
#include <stdlib.h>
#define MAX 15

int main()
{
    int maximum = 10;
    char buf[maximum];

    printf("Enter a string: ");
    gets(nbuf);
    printf("string is %s", buf);

    return 0;
}
